# Python Collections Comparison

| Data Structure | Syntax Example | Ordered | Mutable | Allows Duplicates | Access Method |
|---------------|----------------|---------|---------|-------------------|---------------|
| List | `fruits = ["apple", "banana", "orange"]` | Yes | Yes | Yes | Index (`fruits[0]`) |
| Dictionary | `student = {"name": "Shea", "age": 36}` | Yes | Yes | Keys must be unique (values may repeat) | Key (`student["name"]`) |
| Set | `numbers = {1, 2, 3, 4}` | No | Yes | No | Membership (`in`) or loop |
| Tuple | `colors = ("red", "blue", "green")` | Yes | No | Yes | Index (`colors[0]`) |
## Python Final Report

This repository contains my Python practice files and final competency report files for FSDI 108.

### Files Included

- `intro.py` - Data types, print function, comments, variables, f-strings, type function, and input function.
- `dog_age_converter.py` - A Python program that converts a dog's age into human years.
- `list.py` - Practice using Python lists, indexes, length, replacing items, removing items, and pop.
- `dictionaries.py` - Practice using dictionaries, key-value pairs, adding, updating, removing, and looping through dictionary items.
- `sets.py` - Practice using sets, adding items, removing items, checking membership, and looping.
- `tuples.py` - Practice using tuples, indexing, length, looping, and understanding immutability.
- `if-else.py` - Practice using conditional statements with if, elif, and else.
- `loops.py` - Practice using for loops, range, and while loops.
- `functions.py` - Practice creating and calling functions, using parameters, return values, and lists.

# FSDI 108 Python

This repository contains my Python coursework and competency report files for FSDI 108.  
The project demonstrates beginner Python concepts including variables, data types, collections, conditionals, loops, functions, and basic user input.

## Skills Demonstrated

- Python syntax
- Variables and data types
- `print()` and `input()`
- f-strings
- Lists, dictionaries, sets, and tuples
- If / elif / else statements
- For loops and while loops
- Functions with parameters and return values
- Git and GitHub workflow
- Project organization

## Files Included

| File | Description |
|---|---|
| `intro.py` | Basic Python syntax, variables, comments, f-strings, type checking, casting, and input. |
| `dog_age_converter.py` | Converts a dog’s age into human years using user input and math. |
| `list.py` | Demonstrates list creation, indexing, updating, removing items, and length. |
| `dictionaries.py` | Demonstrates key-value pairs, adding, updating, removing, and looping through dictionary items. |
| `sets.py` | Demonstrates set creation, adding, removing, membership checks, and looping. |
| `tuples.py` | Demonstrates tuple creation, indexing, length, looping, and immutability. |
| `if-else.py` | Demonstrates conditional logic using if, elif, and else. |
| `loops.py` | Demonstrates for loops, range, and while loops. |
| `functions.py` | Demonstrates reusable functions, parameters, return values, and lists. |

## Project Theme

I personalized several examples using plant care and gardening concepts to make the practice files more meaningful while still demonstrating the required Python fundamentals.

## Author

Shea Mullin